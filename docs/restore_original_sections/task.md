# タスクリスト (Task List)

## 完了したタスク
- [x] 原氏デザインのオリジナルLPから不足している各セクション（解決策、導入シナリオ、導入の流れ、料金・比較、セキュリティ、デメリット説明、引き継ぎ、相性確認、想い、FAQ、クロージング）のHTMLを抽出する。
- [x] 抽出したHTML内の画像パスを、Astro環境に適合する相対パス（`../../assets/`）に修正する。
- [x] `TemplateA.astro` の省略コメント部分に抽出・修正した全HTMLを挿入する。
- [x] `./build.sh` を実行してAstroの静的ビルドが正常に行えるか検証する。
- [x] ローカルサーバー（`npm run dev`）で表示を確認し、スクリーンショットおよびスクロール動画を撮影する。

## 次のステップ（検討事項）
- [ ] 追加した各セクションをそれぞれ独立したAstroコンポーネント（例：`LPFAQ.astro`、`LPSecurity.astro` 等）に分割するかどうかを決定する。
- [ ] 各セクションの内容を、MarkdownのFrontmatterから動的に制御（例：FAQの質問・回答リストを配列で渡す等）できるようにスキーマ（`content.config.ts`）を拡張するか検討する。
- [ ] （必要であれば）原氏から追加提供される画像素材を `assets/` フォルダに適切に配置し直す。
